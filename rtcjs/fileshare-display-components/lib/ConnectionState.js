import React from 'react'

const ConnectionState =({connectionState})=>{

    return (<div style={{height:"100%",width:"100%"}} className="bg-info">{connectionState}</div>)
}

export default ConnectionState